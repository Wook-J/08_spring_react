JS 에서는 실제 DOM을 이용(document.get~~~ )

React를 사용할 때는 가상 DOM을 이용하므로 document.get~~으로 얻어오는 것이 아님

Mutates : 원본을 변경함
-> React 에서는 함부로 건들면 안됨 (React 가 반응할 수 있음)

---------------------------------------------------------------------------
editRecipe 부분에서 이미지 수정할 때 경로에서 '/' 빠지는거 추가하기